# Development Logs

2026-06-16 17:15:51 - Started AsyncDataLogger phase 2-5 implementation pass. Purpose: replace the row/variant decoded storage with preallocated typed column batches, reuse ODBC parameter-array buffers where practical, preserve C++17 and public write/autoWrite APIs, and keep SQL/ODBC work outside the async producer path. Initial observation: `AsyncLogger/Instructions.md` supersedes the older row-oriented/variant architecture notes, so the implementation will follow the phase 5 typed-column direction unless a stronger contradiction appears.

2026-06-16 17:22:01 - Started first verification pass after the typed-column refactor. Purpose: scan for stale `std::variant`, `FieldValue`, `DecodedRow`, and old backend signatures before compiling. Rough output/result before build: source-level stale-symbol scan found no variant/row-model usage in compiled code; remaining mentions are in `AsyncLogger/Instructions.md`, which documents the phased migration itself. Next observation: run Visual Studio builds and fix compile/link errors from the interface change.

2026-06-16 17:31:34 - Verification environment pass for the remaining Release solution builds. Purpose: rerun the broader Release builds after AsyncLogger tests passed, using the same MSBuild toolchain. Rough output/result: the plain `msbuild` command was not on the PowerShell PATH in this shell and the login profile also reported execution-policy warnings; `vswhere` located MSBuild at `C:\Program Files (x86)\Microsoft Visual Studio\2019\Community\MSBuild\Current\Bin\MSBuild.exe`. Next observation: rerun the Release builds through the resolved MSBuild path.

2026-06-16 17:34:38 - Final verification pass for AsyncDataLogger phases 2-5. Purpose: confirm the no-variant typed-column implementation, reusable ODBC parameter-array buffers, C++17 builds, SQL smoke behavior, and pressure performance. Rough output/result: compiled source scan found no `std::variant`, `<variant>`, `FieldValue`, `DecodedRow`, `decodeRow`, or `std::get<` usage outside `AsyncLogger/Instructions.md`; AsyncLogger Debug and Release tests built and `--run-tests` passed; Release pressure test passed with `producer_samples=100000 accepted=100000 dropped=0 p99_ns=900 p999_ns=2600 max_ns=423900 stats_try_push_max_ns=423800 max_queue_depth=2771 flush_max_ns=200 rows_inserted=100000`; `DataLoggerSolution.sln` Debug/Release x64 built; `ExampleAppLib` and `ExampleAppLibC` Debug built and their Release project builds passed with only LTCG PDB `LNK4020` debug-symbol warnings. SQL smoke was attempted with the approved connection string but failed before table creation because ODBC Driver 18 reported encryption/client credential and localhost connection errors; cleanup also could not connect, and no smoke table was created to remove. Next observation: no considerable pressure-test performance drop was observed, so no phase was skipped; the remaining SQL blocker is connection-string/driver environment configuration, not the async typed-batch path.

2026-06-16 17:40:28 - Retried the approved real SQL smoke test to diagnose the previous failure. Purpose: distinguish logger/backend behavior from local SQL Server or ODBC environment issues. Rough output/result: `AsyncLoggerTests.exe --real-sql` failed again before table initialization with ODBC Driver 18 diagnostics `Encryption not supported on the client` and `SSL Provider: No credentials are available in the security package`; the connection string already contained `Encrypt=no` and `TrustServerCertificate=yes`, `127.0.0.1:1433` was reachable, Driver 17 and the legacy SQL Server ODBC driver failed with the same SSL/security pattern, and `sqlcmd` failed the same way with both default and explicit trust/encryption flags. Cleanup could not connect and no smoke table was created. Next observation: this is an external SQL Server/Schannel/ODBC connection negotiation blocker rather than a DataLogger insert or async typed-batch failure.

2026-06-16 17:45:59 - Recorded user-reported real SQL smoke success after the local connection blocker cleared outside this shell. Purpose: update the verification trail with the latest SQL result from `.\AsyncLogger\tests\x64\Release\AsyncLoggerTests.exe --real-sql`. Rough output/result: the user reported `AsyncLogger Phase 1 checks passed.`, which means the async SQL smoke path initialized SQL, inserted and verified the smoke rows, and completed its cleanup expectation. Next observation: the earlier failure was environmental/transient for this assistant shell; the implementation is now confirmed by mock tests, pressure tests, builds, and a successful real SQL smoke run.

2026-06-16 17:48:58 - Implemented a test-harness follow-up for SQL connection path robustness and missing phase coverage. Purpose: allow real SQL smoke runs to get the connection string from `--connection-string`, `--connection-string-file`, `SQLLOGGER_CONNECTION_STRING`, or ignored `AsyncLogger/tests/sql_connection.txt`, avoid redundant cleanup failures when initialization never created a table, and add no-SQL checks for all numeric typed-column decoding, generated split-schema autoWrite, and full/partial repeated batch capacity stability. Rough output/result: source patch applied to `AsyncLogger/tests/main.cpp` and `.gitignore`; next observation is to rebuild Debug/Release and rerun the expanded tests plus pressure.

2026-06-16 17:51:25 - Verified the connection-path and missing-test harness follow-up. Purpose: build the async tests and run the expanded no-SQL, pressure, and SQL-smoke paths. Rough output/result: Debug and Release `AsyncLoggerTests.sln` builds passed with 0 warnings and 0 errors; Debug `--run-tests` passed; Release `--run-tests` passed after making generated schema directories process-unique to avoid parallel test races; Release `--pressure` passed with `producer_samples=100000 accepted=100000 dropped=0 p99_ns=800 p999_ns=1100 max_ns=68000 stats_try_push_max_ns=67600 max_queue_depth=4592 flush_max_ns=200 rows_inserted=100000`; Release `--real-sql` in this assistant shell still failed at ODBC connection initialization with the same encryption/client credential diagnostics, but no cleanup failure was emitted because no table was created. Next observation: real SQL can now be run from the passing IDE terminal with the existing env var, an explicit `--connection-string`, `--connection-string-file`, or ignored `AsyncLogger/tests/sql_connection.txt`.

2026-06-19 17:08:04 - Inspected the root Visual Studio solution/source correlation. Purpose: answer which sources `DataLoggerSolution.sln` is connected to without changing build architecture. Rough output/result: the solution contains only the `DataLogger` project at `DataLogger.vcxproj`; that project directly compiles `DataLoggerCore` sources, `SqlServerBackend` sources, and `ExampleApp\main.cpp`, with `ExampleApp\test.cpp` and `ExampleApp\schemas\imu_data.csv` listed as non-compiled items. Next observation: `AsyncLogger`, standalone `DataLoggerCore`, standalone `SqlServerBackend`, `ExampleAppLib`, and `ExampleAppLibC` have separate project or solution files and are not part of `DataLoggerSolution.sln`.

2026-06-19 17:17:15 - Implemented a layout-only repository reorganization. Purpose: move retained example apps and example solutions under `examples/`, keep only `README.md` among root markdown files, and update project/runtime/documentation paths without changing logger behavior. Rough output/result: moved `ExampleApp`, `ExampleAppLib`, `ExampleAppLibC`, `DataLoggerSolution.sln`, and `DataLogger.vcxproj*` into `examples/`; moved root agent/context markdown and async instructions into `docs/`; patched Visual Studio project and solution relative paths plus schema-directory strings and active documentation. Next observation: run path scans and Visual Studio builds to catch any stale references introduced by the move.

2026-06-19 17:21:31 - Verified the layout-only repository reorganization. Purpose: confirm moved example solutions still build, root markdown cleanup is complete, and schema path changes did not break async tests. Rough output/result: root markdown listing showed only `README.md`; `examples\DataLoggerSolution.sln`, `examples\ExampleAppLib\ExampleAppLib.sln`, `examples\ExampleAppLibC\ExampleAppLibC.sln`, and `AsyncLogger\tests\AsyncLoggerTests.sln` all built Debug x64 with 0 warnings and 0 errors; `AsyncLoggerTests.exe --run-tests` passed with `AsyncLogger Phase 1 checks passed.`; literal stale-path scans found no old runtime `schemaDirectory = "ExampleApp..."` or `current / "ExampleApp"` lookups. A malformed exploratory `rg` command briefly escaped the workspace and was killed; it produced no project-relevant signal. Next observation: the remaining old path mentions are historical notes or valid project-internal relative paths.

2026-06-19 17:22:27 - Updated README folder layout documentation. Purpose: explain the cleaned repository tree and give brief definitions for the main folders and example projects. Rough output/result: replaced the compact `Project Layout` bullet list with a small tree and concise definitions for `DataLoggerCore`, `SqlServerBackend`, `AsyncLogger`, `examples`, `tools`, and `docs`. Next observation: verify the README section and root markdown cleanup after the documentation-only change.

2026-06-19 17:48:53 - Inspected the moved monolithic example project entry source. Purpose: answer which source file acts as the executable entry point for `examples\DataLogger.vcxproj`. Rough output/result: the project directly compiles core and backend implementation files plus `ExampleApp\main.cpp`; no custom linker entry point is declared in the project file, so the standard C++ `main()` in `examples\ExampleApp\main.cpp` is the executable entry point. Next observation: `examples\ExampleApp\test.cpp` remains listed as a non-compiled item only.

2026-06-19 17:52:54 - Moved the retained monolithic example solution/project into `examples\ExampleApp`. Purpose: keep `DataLoggerSolution.sln` and `DataLogger.vcxproj*` with the example app source they build, and clarify that `main()` rather than project source order defines the executable entry point. Rough output/result: moved `DataLoggerSolution.sln`, `DataLogger.vcxproj`, `.filters`, and `.user` from `examples\` into `examples\ExampleApp\`; patched project and filter relative paths so `main.cpp`, `test.cpp`, and `schemas\imu_data.csv` are local to `ExampleApp`, while core/backend paths point back to the repo root. Next observation: build the moved solution and scan docs for stale `examples\DataLoggerSolution.sln` references.

2026-06-19 17:53:54 - Verified the monolithic example solution/project move into `examples\ExampleApp`. Purpose: confirm the moved solution still builds and active documentation points at the new path. Rough output/result: `examples\ExampleApp\DataLoggerSolution.sln` built Debug x64 with 0 warnings and 0 errors, producing `examples\ExampleApp\x64\Debug\DataLogger.exe`; active docs were updated to the new solution/project path, and stale `examples\DataLoggerSolution.sln` matches were limited to historical dev-log entries. Next observation: `main.cpp` is the compiled entry translation unit, `test.cpp` remains a non-compiled item, and there is no custom project `EntryPointSymbol`.

2026-07-15 22:18:13 - Inspected the existing-table policy path for a possible continue-current-table logging mode. Purpose: identify the code, docs, examples, and test projects affected by adding a non-recreating table lifecycle option. Rough output/result: `ExistingTablePolicy` currently has only `Drop` and `RenameWithTimestampSuffix`; `DataLogger` passes it into `IDBBackend::initializeTables`, `SqlServerOdbcBackend` always creates a fresh table after drop/rename handling, and both C facades translate the two-value enum. Next observation: adding a continue mode requires updating the requirements/design docs because they currently require two policies and fresh table creation after existing-table handling.

2026-07-15 22:25:50 - Implemented continue-current-table policy support. Purpose: update requirements to match the new explicit append/continue option, keep the default config value unchanged, and add the necessary C++/C API and SQL Server backend behavior. Rough output/result: added `ExistingTablePolicy::ContinueCurrentTable` and `DATALOGGER_EXISTING_TABLE_POLICY_CONTINUE_CURRENT_TABLE`, updated core and async C enum conversion, added SQL Server table-existence and metadata-validation logic before reusing an existing table, made timestamp-index creation idempotent, and extended the real SQL smoke test to run a drop-created table followed by a continue-current-table append pass. Next observation: run Visual Studio 2019 builds and SQL-backed tests.

2026-07-15 22:28:32 - Verified continue-current-table changes with Visual Studio 2019 tooling and local test binaries. Purpose: build the changed projects, run no-SQL regression checks, and attempt the requested live SQL Server smoke. Rough output/result: `AsyncLogger\tests\AsyncLoggerTests.sln` rebuilt Debug and Release x64 successfully with 0 errors and the existing static-library PDB `LNK4099` warnings; Debug and Release `AsyncLoggerTests.exe --run-tests` passed; `examples\ExampleApp\DataLoggerSolution.sln` rebuilt Debug x64 with 0 warnings and 0 errors; default-policy scans confirmed `RenameWithTimestampSuffix` remains the C++ and C default and `ContinueCurrentTable` is used only by the new append smoke path/docs. The live SQL run reached `tcp:127.0.0.1,1433` and confirmed the port is reachable, but Driver 18, Driver 17, and the legacy SQL Server ODBC driver all failed before table initialization with SSL/security credential diagnostics. Next observation: the remaining SQL blocker is local ODBC/Schannel authentication negotiation, not the logger table-policy implementation.

2026-07-26 22:31:22 - Retried the live SQL continue-current-table smoke test. Purpose: reattempt the SQL-backed verification after the user reported the SQL service was ready. Rough output/result: `AsyncLogger\tests\x64\Release\AsyncLoggerTests.exe --real-sql` failed before table initialization with ODBC Driver 18 diagnostics `Encryption not supported on the client`, `SSL Provider: No credentials are available in the security package`, and `Client unable to establish connection`; `Test-NetConnection 127.0.0.1 -Port 1433` still reported `TcpTestSucceeded=True`. Next observation: run the same test command from the user's working SQL/Visual Studio terminal or provide an alternate connection string/driver once the local ODBC security negotiation is corrected.

2026-07-26 22:38:47 - Disabled live SQL smoke-table cleanup for manual inspection. Purpose: leave `[dbo].[async_phase1_smoke]` in `SensorDataDB` after `--real-sql` so the user can verify the continue-current-table append result. Rough output/result: commented out the `cleanupSmokeTable(connectionString)` call at the end of `testRealSqlSmoke`, added an informational print that the table is being left in place, and rebuilt `AsyncLogger\tests\AsyncLoggerTests.sln` Release x64 successfully with 0 errors and the existing static-library PDB `LNK4099` warnings. Next observation: after the user runs the rebuilt test from the working SQL terminal, `[dbo].[async_phase1_smoke]` should remain with 2000 rows.

2026-07-26 22:43:15 - Restored live SQL smoke-table cleanup after manual verification. Purpose: return the `--real-sql` harness to its normal self-cleaning behavior now that the user confirmed the append result. Rough output/result: re-enabled `cleanupSmokeTable(connectionString)` at the end of `testRealSqlSmoke` and rebuilt `AsyncLogger\tests\AsyncLoggerTests.sln` Release x64 successfully with 0 errors and the existing static-library PDB `LNK4099` warnings. Next observation: future successful real SQL smoke runs should again remove `[dbo].[async_phase1_smoke]`.

2026-07-26 23:10:56 - Recorded and analyzed the user-run SQL Server index benchmark for the optional nonclustered columnstore mode. Purpose: compare rowstore timestamp indexing with a nonclustered columnstore index while logging 1,200,000 IMU rows in batches of 1,000. Rough output/result: rowstore initialization took 138.96 ms, insert and flush took 55,852.84 ms at 21,485.03 rows/s, and the aggregate query took 669.97 ms; nonclustered columnstore initialization took 112.20 ms, insert and flush took 77,604.09 ms at 15,463.10 rows/s, and the same query took 523.71 ms. The columnstore mode reduced query time by 21.83% (146.26 ms) but increased insert time by 38.94% (21,751.25 ms) and reduced ingestion throughput by 28.03%. Ignoring initialization variance, approximately 149 equivalent aggregate queries would be required to recover the extra insert cost. Next observation: retain rowstore timestamp-only mode as the default for write-heavy IMU logging and enable the optional nonclustered columnstore mode when repeated analytical-query performance is more important than maximum ingestion throughput.

2026-07-27 00:15:37 - Recorded and analyzed the user-run AsyncDataLogger SQL Server index benchmark. Purpose: compare both SQL Server index modes through the complete async queue, worker, batching, flush, and aggregate-query path using 1,200,000 IMU rows, batches of 1,000, and a queue capacity of 8,192. Rough output/result: rowstore initialization took 149.99 ms, end-to-end insert and flush took 53,944.46 ms at 22,245.10 rows/s, the aggregate query took 877.38 ms, and queue-full submissions were retried 52,381,603 times; nonclustered columnstore initialization took 147.94 ms, insert and flush took 99,847.18 ms at 12,018.37 rows/s, the query took 594.76 ms, and queue-full submissions were retried 74,691,833 times. Within this run, columnstore reduced query time by 32.21% (282.62 ms) but increased ingestion time by 85.09% (45,902.72 ms), reduced throughput by 45.97%, and increased queue-full retry pressure by 42.59%. The retry counts are backpressure attempts rather than lost rows because the harness retries until accepted and validates all requested rows. Ignoring the nearly identical initialization times, approximately 163 equivalent aggregate queries would be required to recover the added ingestion cost. Next observation: keep rowstore timestamp-only as the default for continuous async IMU capture; use nonclustered columnstore only for workloads that can accept roughly half the ingestion throughput and will repeatedly query the accumulated telemetry.
